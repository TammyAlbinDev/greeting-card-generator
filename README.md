# My Greeting Card Generator

Enter a few words and the generator turns them into a greeting card, with a secret surprise after five cards.

## How it's built

- `index.html` + `card.js` — the page
- `scripts/` — the earlier terminal-based greeting card generator and practice scripts

## Live

- Paste your GitHub Pages URL here